import styled from 'styled-components';
import { GlobalBackButton } from '../css.global';


export const ShoppingDetailsViewWrapper = styled.div`

    display: flex;
    flex-direction: column;
    gap: 2rem;

`

export const ShoppingDetailsBackButton = styled.button`

    ${GlobalBackButton}

`